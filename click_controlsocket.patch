diff --git a/elements/userlevel/controlsocket.cc b/elements/userlevel/controlsocket.cc
index 6d31901..24779b0 100644
--- a/elements/userlevel/controlsocket.cc
+++ b/elements/userlevel/controlsocket.cc
@@ -547,6 +547,10 @@ ControlSocket::write_command(connection &conn, const String &handlername, String
   if (_read_only)
     return conn.message(CSERR_PERMISSION, "Permission denied for '" + handlername + "'");
 
+/* OpenBox patch to allow hotswap of any config */
+#undef LARGEST_HANDLER_WRITE
+/* End OpenBox patch */
+
 #ifdef LARGEST_HANDLER_WRITE
   if (data.length() > LARGEST_HANDLER_WRITE)
     return conn.message(CSERR_DATA_TOO_BIG, "Data too large for write handler '" + handlername + "'");
